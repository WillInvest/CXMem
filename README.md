---
type: project-readme
project: cxmem
author: claude
date: 2026-05-06
status: confirmed
---

# CXMem

CXMem designs a hierarchical memory system for Claude Code agents working over many sessions.

Read `STATE.md` for current status and `CLAUDE.md` for project rules.

Design history from `/claudex:think` lives outside this repo at `${HOME}/vault/projects/claudex/specs/` and `${HOME}/vault/projects/claudex/audits/`.

`${HOME}/vault/projects/claude-memory/` is related prior research, but it is separate from this project until R1 reconciles it.

The memory-system implementation is intentionally outside the vault to keep concerns clean.
